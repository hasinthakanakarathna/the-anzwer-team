function managementText(value) { return String(value ?? '').replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[character])); }
async function loadManagementReports() { const response = await CsrfClient.getJson('/api/v1/reports/management'); if (!response.ok) return; const data = await response.json(); document.getElementById('managementReports').innerHTML = data.reports.length ? data.reports.map((report) => `<article class="submitted-report"><div class="submitted-report-head"><div><strong>${managementText(report.full_name || report.username)}</strong><span>${managementText(report.employee_id || report.username)}</span></div><time>${managementText(new Date(report.created_at).toLocaleString())}</time></div><pre>${managementText(report.content)}</pre></article>`).join('') : '<p class="empty-state">No employee reports have been submitted.</p>'; }
loadManagementReports();
